/// Development routing config ///
// export const DIST = "";
// export const FILE = "";
// export const INDEX_PAGE_PATH = DIST + "/" + FILE;

export const DIST = "/dist/";
export const FILE = ".html";
export const CREATE_KEY_PAGE_PATH = DIST + "createKey" + FILE;
export const INDEX_PAGE_PATH = DIST + "index" + FILE;
export const ISSUE_PAGE_PATH = DIST + "issue" + FILE;
export const PRESENT_PAGE_PATH = DIST + "present" + FILE;
export const RESULT_PAGE_PATH = DIST + "result" + FILE;
export const SCANNER_PAGE_PATH = DIST + "scanner" + FILE;
export const LOAD_REQUEST_PAGE_PATH = DIST + "loadRequest" + FILE;
